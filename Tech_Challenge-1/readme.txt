
#Created resource diagram in draw.io

#"3-tier.jpg" is uploaded in same repo.

#By "terraform" we can provision aws resources which is mentioned inside architecture diagram.

#For web and application server tier EC2 instance is required.So uploaded the terraform script.

#For database tier, RDS(postgress) and Redis terrform scripts are uplaoded.

#By terraform scripts we can reproduce the resources again.
